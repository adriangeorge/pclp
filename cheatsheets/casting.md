# Type Casting

| `Funcție` | Descriere |
|---|---|
| `int(valoare)` | Convertește `valoare` într-un număr întreg (integer). |
| `float(valoare)` | Convertește `valoare` într-un număr zecimal (float). |
| `str(valoare)` | Convertește `valoare` într-un șir de caractere (string). |
